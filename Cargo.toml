[package]
name = "psx-doodlejump"
version = "0.1.0"
edition = "2024"

[dependencies]
psx = { path= "/home/safonoff/Tools/psx-sdk-rs/psx"}

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
    'cfg(feature,values("nonexistent_feature"))',
] }
